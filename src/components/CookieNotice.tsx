import React from 'react';

// This component is deprecated as we now use Ketch for consent management
// Keeping this file for backwards compatibility but it renders nothing
const CookieNotice = () => {
  return null;
};

export default CookieNotice;
